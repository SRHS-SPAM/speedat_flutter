import 'package:flutter/material.dart';

class CommunityBest extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('인기 글'),
      ),
      body: Center(
        child: Text('여기는 인기 글 페이지입니다.'),
      ),
    );
  }
}
